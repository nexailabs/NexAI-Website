---
import Italic from './Italic.astro';
import { site } from '../../config/site';
---

<section class="ph-bottom-cta">
	<div class="ph-bottom-cta__copy">
		<h3 class="ph-bottom-cta__h3">
			Want this <Italic>running</Italic>, not just sitting in a tab?
		</h3>
		<p class="ph-bottom-cta__p">
			We'll wire any prompt, SOP, or skill into your agents and hand you the running system. Two
			weeks, fixed price.
		</p>
	</div>
	<a href={site.bookingUrl} data-open-cal class="ph-bottom-cta__btn">
		Catch us
		<svg
			width="13"
			height="13"
			viewBox="0 0 24 24"
			fill="none"
			stroke="currentColor"
			stroke-width="2.2"
			stroke-linecap="round"
			stroke-linejoin="round"
		>
			<line x1="5" y1="12" x2="19" y2="12"></line>
			<polyline points="13 6 19 12 13 18"></polyline>
		</svg>
	</a>
</section>

<style>
	.ph-bottom-cta {
		margin-top: 96px;
		margin-bottom: 80px;
		padding: 40px 48px;
		border: 1px solid var(--color-border);
		border-radius: 10px;
		display: flex;
		align-items: center;
		gap: 32px;
		background: rgba(255, 255, 255, 0.012);
	}
	.ph-bottom-cta__copy {
		flex: 1;
	}
	.ph-bottom-cta__h3 {
		/* h3 element recipe supplies font-family / weight / line-height. */
		margin: 0 0 8px;
		letter-spacing: -0.015em;
	}
	.ph-bottom-cta__p {
		margin: 0;
		font-size: var(--type-lead-size);
		line-height: var(--type-lead-line);
		color: var(--color-text-muted);
		max-width: 560px;
	}
	.ph-bottom-cta__btn {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		padding: 14px 22px;
		background: var(--brand-teal-bright);
		color: var(--color-bg);
		border-radius: var(--radius-full);
		font-size: var(--text-btn);
		font-weight: var(--fw-medium);
		text-decoration: none;
		flex-shrink: 0;
	}

	@media (max-width: 720px) {
		.ph-bottom-cta {
			flex-direction: column;
			align-items: flex-start;
			padding: 32px 24px;
			gap: 24px;
		}
		.ph-bottom-cta__h3 {
			font-size: 1.55rem;
		}
	}
</style>
